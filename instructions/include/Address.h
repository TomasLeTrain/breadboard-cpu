#include <cstdint>

class Address {
private:
  void decodeAddress(uint16_t address) {
    // TODO: implement
  }

public:
  Address(uint16_t address) { decodeAddress(address); }

  // TODO: methods to get instruction data
};
