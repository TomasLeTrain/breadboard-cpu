# Instruction Generator (for now)
rust program that generates the hardware actions required to implement the instruction set (also generated by the program).

Overly complicated to maximize the capability of the cpu.
